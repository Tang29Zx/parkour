# 独立单箱 A/B/C 课程需求

## 用户目标

让从 rough `model_2000.pt` 初始化的 Go2 先分别学会前脚踩顶、后脚踩顶和完整越箱落地，避免在箱前等待或通过摔倒减少持续惩罚。

## 验收标准

- 单箱课程按 A（前脚踩顶）、B（后脚踩顶）、C（完整过箱落地）运行。
- 阶段进度只能首次获得，后退或重复抬脚不能刷奖励。
- 箱前连续 2.5 秒未刷新任务进度时，以停滞失败提前结束。
- 严重撞击、普通失败、停滞和自然未完成的实际终局分值分别为 `-50/-45/-40/-40`。
- 阶段 A/B 达到稳定成功门槛后自动升级，课程状态可随 checkpoint 恢复。
- Critic-only 预热不计入阶段 A 的最短训练轮数。
- 单箱从 rough `model_2000.pt` 一次性迁移，重置 Critic 和优化器，预热期间冻结 Actor；KL 关闭。
- 五箱和 debug 任务行为不变。

## 非目标

- 本轮不追求精确 `0.5 m/s` 控速或漂亮动作。
- 不修改 PPO/GAE 核心公式，不加入域随机化，不删除旧日志和 checkpoint。

## 风险与回滚

- 课程过早升级会导致后脚样本仍不足，因此升级要求连续有效统计窗口。
- 旧单箱 checkpoint 不含新课程状态，普通恢复应报错，避免静默回到阶段 A。
- 回滚只需恢复本次涉及的单箱配置、环境、runner 和测试文件；五箱配置不受影响。
