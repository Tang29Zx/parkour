# 独立单箱 A/B/C 课程技术规格

## 状态机

- 阶段 A：箱高在 `0.08～0.12 m`，前脚真实踩顶累计 2 步即结束本局并计阶段成功。
- 阶段 B：保留前脚规则；后脚真实踩顶累计 2 步即结束本局并计阶段成功。
- 阶段 C：恢复越过箱尾和稳定落地规则；箱高随课程逐步提升到 `0.20 m`。
- 每个阶段至少保持 100 iteration，连续两个有效窗口、每窗口至少 256 个 episode、阶段成功率不低于 `0.65` 后升级；阶段 A 从 Critic-only 预热结束后开始计时。

## 单调任务进度

每个环境维护不可下降的阶段高水位，依次表示：接近箱子、前脚净空、前脚真实踩顶、机身前进、后脚净空、后脚真实踩顶、越过箱尾和稳定落地。奖励只读取高水位增量。

实际累计奖励上限：接近 `+2`、前脚净空 `+2`、前脚踩顶 `+6`、机身前进 `+3`、后脚净空 `+2`、后脚踩顶 `+8`、过箱 `+10`、稳定落地 `+15`。配置权重按 `dt=0.02` 乘以 50。

## 终止与监控

- 进入箱前区域后，连续 125 个控制步没有刷新任务高水位，触发停滞失败。
- 严重机身撞击实际 `-50`，普通失败 `-45`，停滞和自然未完成 `-40`。
- 日志记录课程阶段、阶段成功率、前后脚踩顶、箱顶深入程度、前脚踩顶后机身推进、前后脚净空、停滞和撞击。

## Checkpoint

- 一次性迁移器从 rough `model_2000.pt` 保留 Actor、Actor GRU、状态估计器、状态估计 GRU 和 `std`，扩展扫描编码器，重置 Critic 与优化器。
- Critic-only 预热 100 iteration；Actor 微调学习率 `2e-5`、clip `0.1`、entropy `0.002`；reference KL 为零。
- checkpoint 保存单箱课程阶段及统计计数；普通恢复缺少状态时直接报错。

## 快速验证

- 聚焦单元测试覆盖阶段解锁、单调进度、停滞、终局分值、课程升阶和恢复。
- 运行 Python 语法检查与 `git diff --check`；不在本地运行 4096 环境或长时间仿真。
