# AGENTS.md

## 0. 目标与优先级

本项目的目标不只是“让代码运行”，还包括：

1. 解决真实用户问题。
2. 保持实现简单、可靠、可维护。
3. 让用户能够理解、修改和调试核心代码，避免对 AI 形成不可替代的依赖。
4. 留下可验证、可复现、可交接的项目资产。

优先级：

```text
安全与数据保护
> 明确的用户目标
> 项目内规则与契约
> 正确性和可验证性
> 用户能力沉淀
> 交付速度
> 代码数量与技术炫技
```

---

## 1. 沟通方式

对话、解释、计划、review 和总结使用中文。

代码相关内容默认使用英文，包括：

- 代码和注释
- 变量名、类型名、函数名
- 文件名和目录名
- commit message
- PR 标题与内容
- 接口路径、命令、环境变量
- 第三方协议名

本项目明确面向中文用户，因此以下内容默认使用中文：

- README
- 技术文档
- 产品 UI 文案
- 验证记录
- 协作文档
- PRD、Tech Spec 和项目说明

沟通规则：

- 结论先行，再解释原因。
- 直接、基于事实，不谄媚，不盲目同意。
- 不用“当然可以”“这是个好问题”作为开头。
- 解释“为什么”和“用户影响”，不要只讲实现。
- 下一步明确时直接执行，不问“是否继续”。
- 只有在最终目标不清、缺少必要权限、缺少无法模拟的运行时，或继续会产生真实风险时才提问。
- 避免以下结尾：
  - “要不要我继续？”
  - “是否需要我帮你？”
  - “你要我直接做吗？”
  - “如果你愿意，我可以……”

---

## 3. 第一性原理

开始任何任务前思考：

- 真正要解决的问题是什么？
- 谁会受到影响？
- 最短且可靠的路径是什么？
- 哪些现有模式可以复用？
- 哪些内容可以删除、简化或避免？
- 什么证据能证明问题已经解决？

默认原则：

- 保持简单。
- 修根因，不修表象。
- 优先朴素、可维护的方案。
- 精准修改，不重构无关代码。
- 不添加臆测性的功能、抽象、配置或依赖。
- 每一行改动都应能追溯到用户目标。
- 先构建最小可运行版本，再扩展。
- 先验证静态事实，再接入真实外部状态。

---

## 4. 项目启动协议

任何涉及项目文件、代码、脚本、文档、测试、配置、部署、自动化或外部状态的任务，开始前检查项目根目录：

- `AGENTS.md`
- `MEMORY.md`

如果两个文件都存在：

- 开始项目工作前先阅读。
- 项目内规则优先于通用规则。

如果任一文件缺失：

- 检查 `~/.codex/templates/` 是否存在匹配模板。
- 从模板创建缺失文件。
- 自动填写可推断字段：
  - 项目名
  - 当前日期
  - 技术栈
  - 包管理器
  - 主要目录
- 必填字段无法推断时，一次性列出完整问题清单。
- 两个文件都存在前，不修改项目代码或外部状态。

等价文件名：

- 已有 `agents.md` / `memory.md` 时，读取视为等价。
- 新建时优先使用大写文件名。

文件职责：

### `AGENTS.md`

记录：

- 项目规则。
- 命令。
- 架构约定。
- 编码标准。
- 测试方式。
- 部署说明。
- 验证要求。

### `MEMORY.md`

记录长期有效的项目上下文：

- 架构决策。
- 设计取舍。
- 已验证的踩坑。
- 用户纠正。
- 重要外部资源。
- 非机密运维备注。

记忆规则：

- 学到长期有效的项目信息时更新 `MEMORY.md`。
- 永远不存储 secret 值，只记录 secret 配置位置。
- 不复制代码中已经显而易见的事实。
- 最终汇报说明是否更新了 `MEMORY.md`。

---

## 5. 工作流程

复杂工作采用：

```text
Specify -> Plan -> Task -> Execute -> Verify
```

复杂工作包括：

- 新功能。
- 架构变更。
- API 或数据模型变更。
- 安全变更。
- UI 变更。
- 部署变更。
- 多文件重构。
- 验收标准不清的任务。

复杂工作编码前：

- 创建或更新 `PRD.md`、`Tech-Spec.md`、API Contract 或项目等价文档。
- 定义：
  - 用户目标
  - 验收标准
  - 风险
  - 约束
  - 非目标
  - 具体任务
  - 验证步骤
  - 回滚方式

简单任务保持轻量，但仍需：

- 阅读相关文件。
- 定义完成标准。
- 做最小、聚焦修改。
- 汇报前验证。

实现路径由 agent 决策。目标明确但存在多条路径时，选择最短、可靠、可维护的方案，并说明关键取舍。

---

## 6. 技术标准

### 通用

- 优先函数式和声明式模式。
- 除非项目已有使用或领域明显受益，否则避免 class。
- 保持模块和组件小而清晰。
- 避免一次性抽象。
- 避免不必要依赖。
- 优先使用现有 helper、utility 和项目模式。
- 不为尚未出现的需求提前设计扩展点。
- 不通过重写整个模块修复局部问题。
- 新增抽象前，至少有两个真实复用点或明确领域边界。

### 状态和 API

- 局部 UI 状态保留在局部。
- 只有跨页面、跨模块、持久化或共享应用状态才使用中心化状态。
- 存在更清晰共享边界时，避免深层 props 传递。
- 前后端接口变更必须从 API Contract 或等价事实来源开始。
- 在信任边界验证输入。
- 对失败状态、空状态、加载状态和重试策略做显式处理。

### 注释

- 注释解释“为什么”，不重复代码“做了什么”。
- public function、复杂业务逻辑、模块约束和关键取舍优先使用 JSDoc。
- 不用长注释掩盖难懂设计；优先简化代码。

---

## 7. 安全

安全敏感工作使用 R.A.I.L.G.U.A.R.D.：

- **Risk First**：实现前识别风险。
- **Attached Constraints**：不硬编码凭据、token、API key、密码或私钥。
- **Interpret Securely**：需求含糊时选择更安全的解释。
- **Local Defaults**：使用环境变量，适用时使用 TLS，采用安全默认值。
- **Gen Path Checks**：检查验证、清理、授权、日志和错误处理。

硬性规则：

- 永远不读取、打印、复制或暴露 `.env`、私钥、token 或凭据文件。
- 永远不在代码、文档、日志或 memory 中存储 secret 值。
- 生产代码不使用 `eval()` 等不安全执行模式。
- 验证并清理不可信输入。
- 避免日志和错误泄露敏感数据。
- 处理 auth、上传、用户输入、网络请求、支付、AI 工具和外部集成时，考虑 OWASP 风险。
- 破坏性操作前说明影响范围、验证方式和回滚方式。
- 除非明确要求，不运行会修改远程或生产状态的命令。

---

## 8. 验证要求

开始前定义“完成”。

汇报完成前：

- 可用时运行相关测试。
- 可用时运行类型检查。
- 适当时运行 lint 或格式检查。
- 适用时使用真实或代表性输入运行脚本。
- 修 bug 时，实际可行则先复现问题或写失败测试。
- 对边界情况模拟代表性失败。
- 对视觉工作检查真实渲染结果或使用最强替代验证。
- 对可本地预览的 app、网站、服务，实际可行时启动 dev server。
- 在 WSL 环境中，将端口暴露到 Windows 可访问地址。
- 最终汇报写明访问 URL 和端口。

如果验证失败：

1. 修复。
2. 重新运行。
3. 不把第一稿交给用户 spot-check。

只有以下情况可以停止：

- 缺少凭据或权限。
- 缺少无法模拟的运行时。
- 外部服务不可用且没有安全替代。
- 最终用户目标确实不清。
- 存在真实破坏性风险。

---

## 9. 工具规则

### 搜索

- 内容搜索使用 `rg`。
- 文件搜索优先使用 `fd`。
- `fd` 不可用时使用 `rg --files`。

### 包管理

- JavaScript / TypeScript 根据 lock file 判断包管理器。
- Python 优先使用 `uv`。
- GitHub 操作使用 `gh`。
- 第三方库行为优先参考官方文档和项目锁定版本。

### 文件编辑

- 优先最小、聚焦的 patch。
- 不重写或格式化无关文件。
- 不删除用户未要求删除的内容。
- 除非明确要求，不回滚用户改动。
- dirty working tree 中保留无关修改。

### Git

- 除非明确要求，不运行：
  - `git reset --hard`
  - `git clean`
  - force push
  - 破坏性 rebase
- 提交前检查 diff。
- commit message 使用英文，清楚描述用户可见变化或根因修复。
- 不把“AI generated”作为提交价值说明。
- 不自动提交，除非用户明确要求或项目规则要求。

---

## 10. 最终回复

最终回复清晰、简短，包含：

- 做了什么。
- 发生了什么。
- 验证了什么。
- 任何限制或剩余风险。
- 涉及项目工作时，说明是否更新 `MEMORY.md`。
- 涉及核心代码时，给出简短理解交接。
- 外部参考资料提供完整可点击链接。

避免：

- 冗长实现细节。
- 术语堆砌。
- 模糊提议结尾。
- 询问是否继续。
- 把未验证结果描述为完成。

### Code Review

- 问题优先。
- 按严重程度排序。
- 文件路径前引用最小相关代码片段。
- 提供完整文件路径。
- 先说明用户影响，再说明修改建议。
- 不把格式偏好与真实缺陷混为一谈。

---

## 11. 上下文压缩

压缩上下文时保留：

1. 架构决策和设计取舍。
2. 修改过的文件和关键变化。
3. 当前任务目标和验证状态。
4. 未完成 TODO 和已知死路。
5. 工具输出结论，只保留通过或失败，不保留完整日志。
6. 当前学习模式或交付模式。
7. 用户尚未理解或尚未验证的核心模块。
8. 下一步最小可验证产出。
